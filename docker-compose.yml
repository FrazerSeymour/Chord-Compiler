version: '3'

services:
    chord-compile:
        image: jekyll/jekyll
        command: jekyll serve
        volumes:
            - "./chord-compile/:/srv/jekyll"
            - "${CHORD_PATH}/:/srv/jekyll/_chords"
        ports:
            - "4000:4000"
